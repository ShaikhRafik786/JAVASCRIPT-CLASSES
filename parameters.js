// const getInterest = (amount, time, rate) => {
// const result = amount * time *rate; 
// return result;
// }

// console.log(getInterest(10000, 2));

const getInterest = (amount, time, rate = 0.1) => {
    const result = amount * time *rate; 
    return result;
    }
    
    console.log(getInterest(10000, 2, 0.2));
    
    










